# project_data

Same as **project** (compute via `computes`, `global_identity` and **networking** remote state) but **no Route53 records**. Use **project** for DNS records that point at this infrastructure.
